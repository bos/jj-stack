The files in this directory are examples of how to use the `--describe-with` feature of the
`jj-stack submit` command.

They deliberately duplicate a lot of structure so that each script stands alone. This makes it
possible to move them around in the filesystem without needing to worry about fragile multi-file
dependencies.
